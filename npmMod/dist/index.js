"use strict";

var chalk = require("chalk"); // const validator = require("validator");
// import chalk from 'chalk';


console.log(chalk.blue("Hello world")); // const res = validator.isEmail("ajay@gmail.com");
// console.log(res ? chalk.green.inverse(res) : chalk.red.inverse(res));